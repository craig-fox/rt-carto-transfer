CREATE TABLE IF NOT EXISTS vw_event_by_grid_daily (
  local_date text,
  travel_month text,
  grid_id text,
  tourism_region_name text,
  all_user_count text,
  verified_user_count text,
  event_count text,
  lon text,
  lat text,
  grid_area text,
  source text
)
