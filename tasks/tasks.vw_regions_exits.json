{
    "tasks":[
        {
          "name":"vw_regions_exits_data_1",
          "table_name":"vw_regions_exits",
          "template": "select * from {} WHERE travel_month = '201809' AND tourism_region_name ~* '^[A-L].+$'"
        },
        {
          "name":"vw_regions_exits_data_2",
          "table_name":"vw_regions_exits",
          "template": "select * from {} WHERE travel_month = '201809' AND tourism_region_name ~* '^[M-Z].+$'"
        }

    ]
}
